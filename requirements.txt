# TV-FLIDS Requirements
# Python 3.10.12 (see .python-version / environment.yml; paper Section VI-D)
# Install: pip install -r requirements.txt

# Core ML
torch==2.1.0
torchvision==0.16.0

# Federated Learning
flwr[simulation]==1.6.0
# Pinned explicitly although it arrives transitively via flwr[simulation]:
# the Flower simulation engine executes every virtual client inside a Ray
# worker, so this version is part of the reproducible surface. On Windows,
# importing PyTorch inside these workers fails with a c10.dll initialisation
# error (see README "Known environment limitation"); the full campaign
# therefore needs Linux or WSL2.
ray==2.6.3

# Data Science
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4
imbalanced-learn==0.11.0
hdbscan==0.8.33

# Visualization
matplotlib==3.8.2
seaborn==0.13.0

# Utilities
pyyaml==6.0.1
tqdm==4.66.1
tensorboard==2.15.1

# Stats
statsmodels==0.14.1